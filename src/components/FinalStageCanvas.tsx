import { Paper, Typography } from "@mui/material";

export function FinalStageCanvas() {
  return (
    <Paper
      elevation={3}
      sx={{ height: "100%", display: "grid", placeItems: "center" }}
    >
      <Typography variant="h6">Final Result (placeholder)</Typography>
    </Paper>
  );
}
